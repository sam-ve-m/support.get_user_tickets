apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: get-user-tickets-fn-vs
  namespace: fission
spec:
  hosts:
    - dev.api.siga.me
  gateways:
    - develop/dev-istio-gateway
  http:
  - match:
    - uri:
        prefix: /router/get_user_tickets
    rewrite:
      uri: /get_user_tickets
    route:
    - destination:
        host: router.fission.svc.cluster.local
        port:
          number: 80
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: get-user-tickets-fn-vs
  namespace: fission
spec:
  hosts:
    - dev.api.siga.me
  gateways:
    - develop/dev-istio-gateway
  http:
  - match:
    - uri:
        prefix: /router/support/get-user-tickets
    rewrite:
      uri: /support/get-user-tickets
    route:
    - destination:
        host: router.fission.svc.cluster.local
        port:
          number: 80